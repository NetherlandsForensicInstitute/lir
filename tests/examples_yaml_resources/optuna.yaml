# Override specific values from the example YAML for testing convenience

data.provider.cache_dir: ${output_path}/../examples_yaml_resources/cache

experiments:
  - name: optimize_kde_bandwidth
    strategy: optuna
    primary_metric: cllr
    n_trials: 1
    lrsystem: ${lrsystem}
    data: ${data}
